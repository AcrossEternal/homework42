@model IEnumerable<NewMusic.Models.LoveMusic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MusicName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MusicWhoName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MusicName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MusicWhoName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MusciID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MusciID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MusciID })
        </td>
    </tr>
}

</table>
